# Visual Checklist

Use this checklist after the agent has finished `restyle` and the user opens the final `.docx` in Windows Word.

## Must Pass

- same-level TOC entries start at the same left position
- the page-number column is visually right-aligned as one clean vertical column
- dot leaders run continuously from the title tail toward the page number
- `摘要`, `ABSTRACT`, `参考文献`, and `致谢` appear and sit at the expected top-level hierarchy
- the first real body chapter starts at page `1`

## High-Risk Rows

- the longest level-3 entries
- entries containing `Folio`
- entries containing `iomap`
- entries containing `buffered write`
- `摘要` and `ABSTRACT`

## What Usually Needs Human Micro-Judgment

- whether a very long title still looks balanced after Word refresh
- whether a borderline long line should be manually shortened in the body heading
- whether the final submission version needs a few last-mile spacing tweaks

## What Counts As Success

Success is not “close enough”.

Success means:

- left side looks level-consistent
- right side page numbers align
- hierarchy is correct
- page numbers match Word's real pagination
